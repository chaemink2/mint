# Mint - 의존성 패키지

# ── 데이터 수집 ────────────────────────
pykrx>=1.0.47               # 코스피/코스닥 히스토리컬 데이터
yfinance>=0.2.38            # 나스닥 데이터
requests>=2.31.0            # KIS API HTTP 클라이언트
websocket-client>=1.7.0     # KIS API WebSocket (실시간)
python-dotenv>=1.0.0        # 환경변수 관리

# ── AI / ML ────────────────────────────
qlib>=0.9.0                 # Microsoft Qlib (팩터 + 파이프라인)
lightgbm>=4.3.0             # LightGBM 예측 모델
torch>=2.2.0                # PyTorch (LSTM 모델)
scikit-learn>=1.4.0         # 전처리, 평가
pandas>=2.0.0
numpy>=1.26.0
ta>=0.11.0                  # 기술적 지표 (RSI, MACD, 볼린저밴드 등)

# ── 대시보드 ────────────────────────────
streamlit>=1.32.0           # 대시보드 UI
plotly>=5.20.0              # 차트

# ── DB ─────────────────────────────────
# SQLite (Python 표준 라이브러리 — 로컬 운영). DATABASE_URL=postgres://... 시 SQLAlchemy 통해 Postgres.
sqlalchemy>=2.0             # DB 추상화 (sqlite ↔ postgres)
psycopg2-binary>=2.9        # Postgres 드라이버 (Cloud Migration 후 활성)

# ── 알림 ────────────────────────────────
# 카카오 알림톡: requests 사용 (별도 SDK 없음)

# ── 스케줄러 ────────────────────────────
apscheduler>=3.10.4         # 주기적 시그널 스캔

# ── 테스트 ─────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
